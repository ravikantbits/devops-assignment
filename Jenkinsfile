pipeline {
    agent any

    stages {
        stage('Initialize') {
            echo 'Initialize'
        }
        stage('Build') {
            echo 'Build'
        }
        stage('Test') {
            echo 'Test'
        }
        stage('Deploy') {
            echo 'Deploy'
        }
    }
}
